{
  "name": "prompt-enhancer",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "bin": {
    "prompt-enhance": "./packages/cli/dist/index.js"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "postinstall": "npm link"
  },
  "devDependencies": {
    "turbo": "^1.11.3"
  }
}
